<UserControl x:Class="WorkitemCreator.TemplateSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WorkitemCreator"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0">
            <Label Content="Parent Template" HorizontalAlignment="Right"></Label>
            <ComboBox HorizontalAlignment="Stretch" Name="ParentWorkItemTemplate"></ComboBox>
        </StackPanel>

        <ScrollViewer Grid.Column="0" Grid.Row="1">
            <StackPanel Name="ChildTemplates">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Name="AddChildWorkitemTemplate" Content="Add Child" Height="25" Margin="4" Click="AddChildWorkitemTemplate_OnClick"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
